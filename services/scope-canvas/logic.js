(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.ScopeCanvas=api})(this,function(){
const TYPES={illustration:{label:'一枚絵',base:3,deliverables:['完成画像（PNG/JPG）','確認用ラフ']},model3d:{label:'3Dモデル',base:7,deliverables:['モデル本体','テクスチャ一式','動作確認用プレビュー']},web:{label:'Webページ',base:5,deliverables:['公開用ファイル一式','スマホ表示対応','操作確認結果']},video:{label:'短尺動画',base:5,deliverables:['完成動画','サムネイル候補']}};
const OPTIONS={source:{label:'編集可能な元データ',points:2,note:'元データの形式と使用ソフトを明記'},commercial:{label:'商用利用',points:1,note:'利用媒体・期間・クレジット表記を合意'},variants:{label:'差分3点',points:3,note:'差分の内容と組み合わせ数を固定'},handoff:{label:'導入・引き継ぎ',points:2,note:'導入先の環境とサポート範囲を固定'}};
function analyze(input){const type=TYPES[input.type]||TYPES.illustration,selected=(input.options||[]).filter(x=>OPTIONS[x]);let points=type.base+selected.reduce((n,k)=>n+OPTIONS[k].points,0)+Math.max(0,(Number(input.revisions)||1)-1);const risks=[];if((Number(input.revisions)||1)>=3)risks.push('修正回数が多め：各回の修正対象をまとめる');if(input.urgent){points+=2;risks.push('短納期：確認待ち時間と素材支給日を先に決める')}if(selected.includes('commercial'))risks.push('商用利用：用途・期間・二次利用の範囲を明記');if(selected.includes('source'))risks.push('元データ納品：使用ソフトと互換性を確認');const deliverables=[...type.deliverables,...selected.map(k=>OPTIONS[k].label)];const checks=[...selected.map(k=>OPTIONS[k].note),'納期、修正回数、納品形式を双方で確認'];return{label:type.label,points,band:points<=5?'小':points<=9?'中':'大',deliverables,risks,checks,revisions:Number(input.revisions)||1,urgent:!!input.urgent}}
function toText(result){return `【依頼範囲メモ：${result.label}】\n規模の目安：${result.band}（範囲ポイント ${result.points}）\n\n■ 納品物\n${result.deliverables.map(x=>'・'+x).join('\n')}\n\n■ 修正\n・${result.revisions}回まで\n\n■ 先に合意すること\n${result.checks.map(x=>'・'+x).join('\n')}\n${result.risks.length?'\n■ 見落とし注意\n'+result.risks.map(x=>'・'+x).join('\n'):''}\n\n※ポイントは価格ではなく、作業範囲の比較用です。`;}
return{TYPES,OPTIONS,analyze,toText};
});
