const $=s=>document.querySelector(s),L=window.LicenseLedger,SLUG='license-ledger',NS='amu-web-service-lab',PINGS=[];let started=false,completed=false,last=null;
function metric(name){const img=new Image();PINGS.push(img);img.onload=img.onerror=()=>PINGS.splice(PINGS.indexOf(img),1);img.src=`https://api.counterapi.dev/v1/${NS}/${SLUG}-${name}/up?t=${Date.now()}`}
metric('view');try{if(!localStorage.getItem(`${SLUG}-counted`)){setTimeout(()=>{metric('visitor');localStorage.setItem(`${SLUG}-counted`,'1')},1200)}}catch(e){}
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(){last=L.analyze($('#ledger').value);const s=last.summary;$('#total').textContent=s.total;$('#ready').textContent=s.complete;$('#licenses').textContent=s.licenses;$('#issues').textContent=s.issueCount;$('#groups').innerHTML=last.groups.map(g=>`<article class="license"><div><b>${esc(g.label)}</b><span>${g.items.length}件</span></div><p>${esc(g.note)}</p><small>${g.items.map(x=>esc(x.asset)).join(' / ')}</small></article>`).join('')||'<p class="empty">素材を入力すると条件別に整理するよ。</p>';$('#warnings').innerHTML=last.issues.length?last.issues.map(x=>`<p class="warning">${esc(x.message)}</p>`).join(''):'<p class="good">必須情報の大きな不足はないよ。公開前に原文も確認してね。</p>'}
$('#analyze').addEventListener('click',()=>{if(!started){started=true;metric('start')}render();$('.results').scrollIntoView({behavior:'smooth',block:'start'})});$('#ledger').addEventListener('input',render);$('#copy').addEventListener('click',()=>{if(!started){started=true;metric('start')}const area=document.createElement('textarea');area.value=L.markdown($('#ledger').value);area.style.position='fixed';area.style.opacity='0';document.body.append(area);area.select();let ok=false;try{ok=document.execCommand('copy')}catch(e){}area.remove();$('#status').textContent=ok?'Markdownをコピーしたよ。':'Markdownを生成したよ。';if(!completed){completed=true;metric('complete')}});render();
