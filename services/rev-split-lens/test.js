const assert=require('assert');const D=require('./logic.js');let n=0;function near(actual,expected,epsilon=.001){assert(Math.abs(actual-expected)<epsilon,`${actual} != ${expected}`);n++}function same(actual,expected){assert.deepStrictEqual(actual,expected);n++}
const input={price:1000,refund:10,platform:20,otherFee:5,recoupCost:100000,lowUnits:100,baseUnits:200,highUnits:400};const people=[{name:'A',share:60},{name:'B',share:40}];const r=D.calculate(input,people);
near(r.netRate,.684);near(r.unitReceipts,684);same(r.breakEvenUnits,147);near(r.shareTotal,100);same(r.low.units,100);near(r.low.gross,100000);near(r.low.refunds,10000);near(r.low.platformFee,18000);near(r.low.otherFee,3600);near(r.low.receipts,68400);near(r.low.recouped,68400);near(r.low.unrecouped,31600);near(r.low.distributable,0);near(r.base.receipts,136800);near(r.base.distributable,36800);near(r.base.payouts[0].amount,22080);near(r.base.payouts[1].amount,14720);near(r.base.unallocated,0);near(r.high.distributable,173600);
const bad=D.calculate({...input,platform:200,refund:-2,otherFee:'x',recoupCost:-5},[{name:'',share:130}]);near(bad.netRate,0);same(bad.breakEvenUnits,null);near(bad.shareTotal,100);same(bad.participants[0].name,'参加者 1');
const partial=D.calculate({...input,recoupCost:0},[{name:'A',share:70}]);near(partial.base.unallocated,partial.base.distributable*.3);
const exported=D.exportData(r,input);same(exported.assumptions.price,1000);same(exported.scenarios.length,3);same(exported.scenarios[1].payouts.A,22080);console.log(`PASS ${n} assertions`);
